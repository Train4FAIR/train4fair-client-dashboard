#version: '3'

#services:
  # APP  ***************************************************************************************
#  node-red-train:
#    image: jbjares/node-red-train:v10
#    ports:
#      - 1880:1880
#    volumes:
#      - "./start.sh:/app/start.sh"
#      - "./settings-cloud41.js:/app/settings-cloud41.js"
#      - "./settings-localhost.js:/app/settings-localhost.js"
#    command: bash -c "/app/start.sh --init true --env cloud41"

version: '3'

services:
  # APP  ***************************************************************************************
  node-red-train:
    image: jbjares/node-red-train:v10
    ports:
      - 1880:1880
    #command: bash -c "cd /app/train-modelling-tool-gofair && npm install && npm run build  && npm start && cd packages/node_modules/node-red && node red.js -s ./client-installation/settings-cloud41.js"
    #command: bash -c "cd /app/train-modelling-tool-gofair && npm install && npm run build  && npm start && cd packages/node_modules/node-red && node red.js -s client-installation/settings-cloud41.js"
    command: bash -c "cd /app/train-modelling-tool-gofair/packages/node_modules/node-red && node red.js -s client-installation/settings-cloud41.js"

